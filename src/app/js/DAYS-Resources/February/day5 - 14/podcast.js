function theCSSpodcastDAY5() {
  // (episodeID, date, description)

  ResourcesSection.innerHTML += theCSSpodcastTemplate(
    `13443002`,
    `Mar 12, 2020`,
    ` Where do scrollbars go, inside or outside the box? In this
    episode Una and Adam answer that and much more in their
    discussion of the CSS box model.`
  );
}
